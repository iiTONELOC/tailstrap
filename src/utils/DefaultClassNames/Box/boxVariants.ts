import { BoxVariants as Variants } from "../../../types";

export default function boxVariants(variant: Variants) {
    switch (variant) {
        case 'row':

            break;

        default:
            break;
    }
}